{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci --no-audit --no-fund --prefer-offline=false",
  "buildCommand": "npx prisma db push && npx prisma generate && next build"
}