{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare yarn@1.22.22 --activate && yarn install --network-timeout 600000 --no-progress",
  "buildCommand": "corepack enable && corepack prepare yarn@1.22.22 --activate && yarn prisma db push && yarn prisma generate && yarn next build"
}